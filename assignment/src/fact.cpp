/*
#include<iostream>
using namespace std;
float factorial(int n);
int main()
{
    int k;
    cout<<"enter the no.";
    cin>>k;
    cout<<"factorial "<<k<<" is "<<factorial(k);
    return 0;
}
float factorial(int n)
{
      if(n==0)

               return 1;
      else
               return(n*factorial(n-1));

}

*/
